---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

# Monitoring stack is deployed via Helm chart (see argocd-apps/monitoring.yaml)
# This directory can contain custom Grafana dashboards and Prometheus rules

# resources:
#   - custom-dashboard.yaml
#   - prometheus-rules.yaml
