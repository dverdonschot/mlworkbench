---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: minio

# MinIO is deployed via Helm chart (see argocd-apps/minio.yaml)
# This directory contains sealed secrets for MinIO

resources:
  - minio-root-credentials-sealed.yaml
