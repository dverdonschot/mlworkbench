---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: tailscale

# Tailscale Operator is deployed via Helm (see argocd-apps/tailscale.yaml)
# This directory contains the OAuth sealed secret for the operator

resources:
  - operator-oauth-sealed.yaml
